const pessoa = {                 ////para declarar um objeto basta declarar uma função e por os dados entre {}
    nome: "Rafael",              /// os dados do objeto são divididos por , (vírgula)
    sobrenome: "Vitor",
    idade: 25,
}

console.log(pessoa);
console.log(pessoa.nome);    // para acessar dados individuais de um objet, basta puxar pelo .(ponto)
console.log(pessoa.sobrenome);
console.log(pessoa.idade);
